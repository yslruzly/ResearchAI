// The route and the marketing copy both import from here, so the advertised
// model can't drift from the one actually called.
export const MODEL_ID = "openai/gpt-oss-120b";

/** human-readable form for body copy */
export const MODEL_LABEL = "Groq (gpt-oss-120b)";
